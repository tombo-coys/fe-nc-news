import React from 'react';
import ArticlesList from './ArticlesList';

const Articles = () => {
    return (
        <section>
            <ArticlesList /> 
        </section>
    );
};

export default Articles;